SecretsTable:
  Type: AWS::DynamoDB::Table
  Properties:
    TableName: ${self:custom.SecretsTable.name}
    BillingMode: PAY_PER_REQUEST
    AttributeDefinitions:
      - AttributeName: uuid
        AttributeType: S
    KeySchema:
      - AttributeName: uuid
        KeyType: HASH